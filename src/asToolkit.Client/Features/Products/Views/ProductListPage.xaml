<Page x:Class="asToolkit.Client.Features.Products.Views.ProductListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:asToolkit.Client.Features.Products.Views"
      xmlns:models="using:asToolkit.Client.Features.Products.Models"
      xmlns:mvux="using:Uno.Extensions.Reactive.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header with NavigationBar -->
    <utu:NavigationBar Grid.Row="0"
                       x:Uid="ProductListPage.NavigationBar">
      <utu:NavigationBar.PrimaryCommands>
        <AppBarButton x:Uid="ProductListPage.AppBarButton.Refresh"
                      Command="{Binding Refresh, ElementName=ProductsFeedView}">
          <AppBarButton.Icon>
            <FontIcon Glyph="&#xE72C;" />
          </AppBarButton.Icon>
        </AppBarButton>
        <AppBarButton x:Uid="ProductListPage.AppBarButton.New"
                      Click="CreateProduct_Click">
          <AppBarButton.Icon>
            <FontIcon Glyph="&#xE710;" />
          </AppBarButton.Icon>
        </AppBarButton>
      </utu:NavigationBar.PrimaryCommands>
    </utu:NavigationBar>

    <!-- Search Box + Filters -->
    <Grid Grid.Row="1"
          Margin="16,12,16,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <Grid Grid.Column="0">
        <TextBox x:Name="SearchBox"
                 x:Uid="ProductListPage.SearchBox"
                 TextChanged="SearchBox_TextChanged"
                 Padding="40,0,12,0" />
        <FontIcon Glyph="&#xE721;"
                  FontSize="16"
                  Foreground="{ThemeResource OnSurfaceVariantBrush}"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Center"
                  Margin="12,0,0,0"
                  IsHitTestVisible="False" />
      </Grid>

      <ToggleSwitch x:Name="IncludeVariantsToggle"
                    Grid.Column="1"
                    x:Uid="ProductListPage.IncludeVariants"
                    Margin="16,0,0,0"
                    VerticalAlignment="Center"
                    Toggled="IncludeVariantsToggle_Toggled" />
    </Grid>

    <!-- Product Table -->
    <mvux:FeedView x:Name="ProductsFeedView"
                   Grid.Row="2"
                   Margin="16,0,16,16"
                   Source="{Binding Products}">
      <mvux:FeedView.ValueTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource OutlineVariantBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource SurfaceBrush}">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <!-- Table Header -->
              <Border Grid.Row="0"
                      Background="{ThemeResource SurfaceVariantBrush}"
                      CornerRadius="12,12,0,0"
                      BorderBrush="{ThemeResource OutlineVariantBrush}"
                      BorderThickness="0,0,0,1">
                <Grid Padding="0">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="100" />
                  </Grid.ColumnDefinitions>

                  <!-- Sku Header -->
                  <Button x:Name="HeaderSku"
                          Grid.Column="0"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="Sku"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="ProductListPage.Header.Sku"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconSku"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>

                  <!-- Name Header -->
                  <Button x:Name="HeaderName"
                          Grid.Column="1"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="Name"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="ProductListPage.Header.Name"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconName"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource PrimaryBrush}"
                                Visibility="Visible" />
                    </StackPanel>
                  </Button>

                  <!-- Manufacturer Header -->
                  <Button x:Name="HeaderManufacturer"
                          Grid.Column="2"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="Manufacturer"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="ProductListPage.Header.Manufacturer"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconManufacturer"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>

                  <!-- Price Header -->
                  <Button x:Name="HeaderPrice"
                          Grid.Column="3"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="Price"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="ProductListPage.Header.Price"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconPrice"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>

                  <!-- MSRP Header -->
                  <Button x:Name="HeaderMsrp"
                          Grid.Column="4"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="Msrp"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="ProductListPage.Header.Msrp"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconMsrp"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>
                </Grid>
              </Border>

              <!-- Table Body with ScrollViewer -->
              <ScrollViewer Grid.Row="1"
                            VerticalScrollBarVisibility="Auto"
                            HorizontalScrollBarVisibility="Disabled">
                <ItemsRepeater x:Name="ProductItemsRepeater"
                               ItemsSource="{Binding Data}">
                  <ItemsRepeater.Layout>
                    <StackLayout Orientation="Vertical" Spacing="0" />
                  </ItemsRepeater.Layout>
                  <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                      <Button Style="{StaticResource TextButtonStyle}"
                              HorizontalAlignment="Stretch"
                              HorizontalContentAlignment="Stretch"
                              Padding="0"
                              CornerRadius="0"
                              Click="ProductRow_Click">
                        <Grid Background="Transparent"
                              Padding="0"
                              BorderBrush="{ThemeResource OutlineVariantBrush}"
                              BorderThickness="0,0,0,1">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="100" />
                          </Grid.ColumnDefinitions>

                          <!-- Thumbnail + Sku -->
                          <StackPanel Grid.Column="0"
                                      Orientation="Horizontal"
                                      Spacing="8"
                                      VerticalAlignment="Center"
                                      Padding="12,8">
                            <Border Width="36"
                                    Height="36"
                                    CornerRadius="6"
                                    Background="{ThemeResource SurfaceVariantBrush}">
                              <Image Source="{Binding ThumbnailBytes, Converter={StaticResource BytesToImageSourceConverter}}"
                                     Stretch="UniformToFill" />
                            </Border>
                            <TextBlock Text="{Binding Sku}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" />
                          </StackPanel>

                          <!-- Name + Type Badge -->
                          <StackPanel Grid.Column="1"
                                      Orientation="Horizontal"
                                      Spacing="8"
                                      VerticalAlignment="Center"
                                      Padding="16,14">
                            <TextBlock Text="{Binding Name}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" />

                            <!-- Variant Parent Badge -->
                            <Border CornerRadius="10"
                                    Padding="8,2"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource PrimaryContainerBrush}"
                                    Visibility="{Binding ProductType, Converter={StaticResource ProductTypeToVisibilityConverter}, ConverterParameter=VariantParent}">
                              <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock Text="{Binding ProductType, Converter={StaticResource ProductTypeToTextConverter}}"
                                           Style="{StaticResource LabelSmall}"
                                           Foreground="{ThemeResource OnPrimaryContainerBrush}" />
                                <TextBlock Style="{StaticResource LabelSmall}"
                                           Foreground="{ThemeResource OnPrimaryContainerBrush}">
                                  <Run Text="(" /><Run Text="{Binding VariantCount}" /><Run Text=")" />
                                </TextBlock>
                              </StackPanel>
                            </Border>

                            <!-- Variant Child Badge -->
                            <Border CornerRadius="10"
                                    Padding="8,2"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource TertiaryContainerBrush}"
                                    Visibility="{Binding ProductType, Converter={StaticResource ProductTypeToVisibilityConverter}, ConverterParameter=Variant}">
                              <TextBlock Text="{Binding ProductType, Converter={StaticResource ProductTypeToTextConverter}}"
                                         Style="{StaticResource LabelSmall}"
                                         Foreground="{ThemeResource OnTertiaryContainerBrush}" />
                            </Border>
                          </StackPanel>

                          <!-- Manufacturer -->
                          <TextBlock Grid.Column="2"
                                     Text="{Binding Manufacturer.Name}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                     VerticalAlignment="Center"
                                     Padding="16,14"
                                     TextTrimming="CharacterEllipsis" />

                          <!-- Price -->
                          <TextBlock Grid.Column="3"
                                     Text="{Binding Price, Converter={StaticResource CurrencyConverter}}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}"
                                     VerticalAlignment="Center"
                                     Padding="16,14"
                                     HorizontalAlignment="Right" />

                          <!-- MSRP -->
                          <TextBlock Grid.Column="4"
                                     Text="{Binding Msrp, Converter={StaticResource CurrencyConverter}}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                     VerticalAlignment="Center"
                                     Padding="16,14"
                                     HorizontalAlignment="Right" />
                        </Grid>
                      </Button>
                    </DataTemplate>
                  </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
              </ScrollViewer>
            </Grid>
          </Border>
        </DataTemplate>
      </mvux:FeedView.ValueTemplate>

      <mvux:FeedView.ProgressTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource OutlineVariantBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource SurfaceBrush}">
            <ProgressRing IsActive="True"
                          Width="48"
                          Height="48"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="48" />
          </Border>
        </DataTemplate>
      </mvux:FeedView.ProgressTemplate>

      <mvux:FeedView.NoneTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource OutlineVariantBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource SurfaceBrush}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16"
                        Margin="48">
              <FontIcon Glyph="&#xE7BF;"
                        FontSize="48"
                        Foreground="{ThemeResource OnSurfaceVariantBrush}" />
              <TextBlock x:Uid="ProductListPage.NoProducts"
                         Style="{StaticResource BodyMedium}"
                         HorizontalAlignment="Center" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </mvux:FeedView.NoneTemplate>

      <mvux:FeedView.ErrorTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource ErrorBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource ErrorContainerBrush}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16"
                        Margin="48">
              <FontIcon Glyph="&#xE783;"
                        FontSize="48"
                        Foreground="{ThemeResource ErrorBrush}" />
              <TextBlock x:Uid="ProductListPage.LoadError"
                         Style="{StaticResource BodyMedium}"
                         Foreground="{ThemeResource OnErrorContainerBrush}"
                         HorizontalAlignment="Center" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </mvux:FeedView.ErrorTemplate>
    </mvux:FeedView>

    <!-- Contextual empty state — overlays the FeedView's None state with an actionable message
         (no channel / import running / import failed / nothing imported yet). Driven from code-behind
         via ProductListModel.ProductsLoaded + GetEmptyStateAsync. Collapsed until products load empty. -->
    <Border x:Name="EmptyStateOverlay"
            Grid.Row="2"
            Visibility="Collapsed"
            CornerRadius="12"
            BorderBrush="{ThemeResource OutlineVariantBrush}"
            BorderThickness="1"
            Background="{ThemeResource SurfaceBrush}">
      <StackPanel HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Spacing="16"
                  MaxWidth="420"
                  Margin="48">
        <FontIcon x:Name="EmptyStateIcon"
                  Glyph="&#xE7BF;"
                  FontSize="48"
                  Foreground="{ThemeResource OnSurfaceVariantBrush}" />
        <TextBlock x:Name="EmptyStateTitle"
                   Style="{StaticResource TitleMedium}"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap" />
        <TextBlock x:Name="EmptyStateMessage"
                   Style="{StaticResource BodyMedium}"
                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap" />
        <Button x:Name="EmptyStateActionButton"
                Style="{StaticResource FilledButtonStyle}"
                HorizontalAlignment="Center"
                Visibility="Collapsed"
                Click="EmptyStateAction_Click" />
      </StackPanel>
    </Border>

    <!-- Pagination Controls -->
    <Border Grid.Row="3"
            Background="{ThemeResource SurfaceBrush}"
            BorderBrush="{ThemeResource OutlineVariantBrush}"
            BorderThickness="0,1,0,0"
            Padding="16,8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Count Info (Left) -->
        <mvux:FeedView Source="{Binding Pagination}"
                       Grid.Column="0">
          <mvux:FeedView.ValueTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Data.CountInfo}"
                         Style="{StaticResource BodySmall}"
                         Foreground="{ThemeResource OnSurfaceVariantBrush}"
                         VerticalAlignment="Center" />
            </DataTemplate>
          </mvux:FeedView.ValueTemplate>
        </mvux:FeedView>

        <!-- Page Navigation (Center) -->
        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Spacing="8">
          <Button x:Name="PreviousPageButton"
                  Style="{StaticResource IconButtonStyle}"
                  x:Uid="ProductListPage.PreviousPage"
                  Click="PreviousPage_Click">
            <FontIcon Glyph="&#xE76B;"
                      FontSize="16" />
          </Button>

          <mvux:FeedView Source="{Binding Pagination}">
            <mvux:FeedView.ValueTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Data.PageInfo}"
                           Style="{StaticResource BodyMedium}"
                           VerticalAlignment="Center"
                           MinWidth="100"
                           TextAlignment="Center" />
              </DataTemplate>
            </mvux:FeedView.ValueTemplate>
          </mvux:FeedView>

          <Button x:Name="NextPageButton"
                  Style="{StaticResource IconButtonStyle}"
                  x:Uid="ProductListPage.NextPage"
                  Click="NextPage_Click">
            <FontIcon Glyph="&#xE76C;"
                      FontSize="16" />
          </Button>
        </StackPanel>

        <!-- Page Size Selection (Right) -->
        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8">
          <TextBlock x:Uid="ProductListPage.ItemsPerPage"
                     Style="{StaticResource BodySmall}"
                     Foreground="{ThemeResource OnSurfaceVariantBrush}"
                     VerticalAlignment="Center" />
          <ComboBox x:Name="PageSizeComboBox"
                    Style="{StaticResource PageSizeComboBoxStyle}"
                    SelectionChanged="PageSizeComboBox_SelectionChanged"
                    SelectedIndex="1">
            <ComboBoxItem Content="10" Tag="10" />
            <ComboBoxItem Content="25" Tag="25" />
            <ComboBoxItem Content="100" Tag="100" />
            <ComboBoxItem Content="500" Tag="500" />
            <ComboBoxItem Content="1000" Tag="1000" />
          </ComboBox>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Page>
